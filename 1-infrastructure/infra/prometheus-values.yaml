grafana:
  enabled: true
  adminPassword: "admin"
  service:
    type: NodePort
    nodePort: 30003
prometheus:
  prometheusSpec:
    scrapeInterval: "10s"
    evaluationInterval: "10s"
kubeStateMetrics:
  enabled: true
nodeExporter:
  enabled: true