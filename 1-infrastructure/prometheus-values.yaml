grafana:
  enabled: true
  adminPassword: "admin"
  service:
    type: NodePort
    nodePort: 30003
prometheus:
  prometheusSpec:
    scrapeInterval: "10s"
    evaluationInterval: "10s"
  service:
    type: NodePort
    nodePort: 30090 
kubeStateMetrics:
  enabled: true
nodeExporter:
  enabled: true
