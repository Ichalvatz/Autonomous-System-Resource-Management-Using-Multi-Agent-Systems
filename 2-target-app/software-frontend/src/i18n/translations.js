/**
 * Translation resources for the application
 * 
 * Translations are split by language for maintainability.
 * Each language file is under 180 lines when considering only translation keys.
 */
import { en } from './locales/en';
import { el } from './locales/el';

export const translations = {
  en,
  el
};
