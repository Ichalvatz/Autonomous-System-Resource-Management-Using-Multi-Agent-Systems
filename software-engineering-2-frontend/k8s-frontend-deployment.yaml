apiVersion: apps/v1
kind: Deployment
metadata:
  name: aiops-frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aiops-frontend
  template:
    metadata:
      labels:
        app: aiops-frontend
    spec:
      containers:
      - name: aiops-frontend-container
        image: aiops-frontend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: aiops-frontend-service
spec:
  type: NodePort
  selector:
    app: aiops-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30002 # Προσοχή: Το backend είναι στο 30001, το frontend στο 30002!
